declare module "hours-objects" {
    export interface HourObject { h: number, m: number }
    export interface InitObject { morningHour: string, lunchHour: string, afternoonHour: string, endOfDayHour: string }
}